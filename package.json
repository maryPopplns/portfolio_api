{
  "name": "portfolio-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "devstart": "open http://localhost:5050 && nodemon ./bin/www",
    "serverstart": "DEBUG=portfolio-api:* npm run devstart",
    "playground": "node utils/dbplayground.js",
    "git": "git add . && git commit && git push origin $(git rev-parse --abbrev-ref HEAD) && echo '\n' && echo $(tput setaf 2)'committed: ' $(tput setaf 1)$(date) && echo '\n'"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.8",
    "morgan": "^1.10.0",
    "winston": "^3.6.0"
  }
}
